import XCTest
@testable import AssesmentDay2Package

final class AssesmentDay2PackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AssesmentDay2Package().text, "Hello, World!")
    }
}
